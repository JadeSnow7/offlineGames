import Foundation

// GameUI module — shared SwiftUI components with Liquid Glass design.

extension Bundle {
  /// Bundle containing localized strings and assets for GameUI.
  public static let gameUI = Bundle.module
}
